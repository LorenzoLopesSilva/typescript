const numbers: number[] = [1, 2, 3, 4, 5];
const names: string[] = ["Lorenzo", "Ingrid", "Rafa"];
const booleans: boolean[] = [true, false];

//metodos
names.push("Lucas");//unshift adiciona no inicio
console.log(names);

numbers.pop();//shift remove o do inicio
console.log(numbers);

